<a href="job_form/0/add" class="btn btn-primary float-right mb-4"><i class="fa fa-plus-circle mr-2"></i>Ajouter un métier</a>
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th class="col-11">Name</th>
                <th class="col-1"></th>
            </tr>
        </thead>
        <tbody>
        <div> 
            {% for job in jobs %}
            <tr>
                <td class="col-11"> {{ job.name }} </td>

                <td class="col-1" >
                    <a href="{{ asset('/job_form/' ~ job.id ~ '/update') }}" class="btn btn-info btn-sm">
                        <i class="fa fa-pencil mr-0"></i>
                    </a>
                </td>
            </tr>
            {% endfor %}
            </div>
           
        </tbody>
    </table>
</div>
<div>
    {{ knp_pagination_render(jobs) }}
</div>